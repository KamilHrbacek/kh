# packages/

Shared code consumed by `apps/` — **not** deployed on its own.

Empty until something is genuinely shared. Likely first tenants:
- `brand/` — tokens, logos, fonts (the brand manual's machine-readable side).
- `ui/` — shared components + their admin.
- `auth/` — KH-RBAC (passkey identity + group-based access).
