# reference/

Archived source + handoffs — **not deployed, not part of the build.**

- `kh-group-webflow/` — the old Webflow export/notes for kh.group (historical).
- `stox-handoff/` — the stox live-data roadmap: `HANDOFF.md`, the backend API contract
  (`stox-backend/`), and design/roadmap notes. Start here when wiring stox to real data.
